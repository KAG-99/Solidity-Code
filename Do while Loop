// SPDX-License-Identifier: GPL-3.0

pragma solidity ^0.8.0;

contract array {
    uint[4] public arr;
    uint public count;

    function loop() public{
        
       do {
        arr[count]=count;
        count++;
        }
        while(count<arr.length);
    }
}
